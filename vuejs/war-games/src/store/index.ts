import { createStore } from 'vuex'
import { msal } from "./modules/msal";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    msal,
  }
})
